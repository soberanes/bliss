<div class="hero-unit">
	<p class="text-header">
    	<span class="titFel">Editar distribuidor</span><br/> 
    		Edita el nombre del distribuidor
    </p>
    <div class="content app_content">
		<?php
		$form->prepare();
		$form->setAttribute('action', $this->basePath('admin-distribuidores/editar-distribuidor'));
		$form->setAttribute('method', 'post');
		$form->setAttribute('class', 'reg-form complete-form')
		?>
		<?php echo $this->form()->openTag($form) ?>
		    <h1><?php echo  $distribuidor ?></h1>

			<?php if(count($form->getMessages())): ?>
				<div class="alert-error">Ha ocurrido un error. Por favor verifique sus datos.</div>
			<?php endif ?>
		    <div class="form-group">
		    	<?php echo $this->formHidden($form->get('distribuidor_id')); ?>
		        <?php echo $this->formRow($form->get('nombre')) ?>
		    </div>
		    
			<div class="form-group">
		    	<a href="<?php echo $this->basePath('admin-sucursales') ?>" class="btn-admin btn--secondary">Cancelar</a>
		    	<input type="submit" class="btn-admin btn--positive" value="Guardar distribuidor" />
		    </div>
		<?php echo $this->form()->closeTag() ?>
	</div>
</div>